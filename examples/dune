(executable
  (name knuth_plass)
  (modules knuth_plass)
  (libraries deque str unix))

(executable
  (name ngrams)
  (modules ngrams)
  (libraries deque))

(executable
  (name string_builder)
  (modules string_builder)
  (libraries deque unix))
